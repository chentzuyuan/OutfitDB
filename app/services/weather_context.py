"""Phase 3 stub for weather fetching. Phase 1 does not call external APIs."""
from datetime import datetime
from typing import Optional, Dict


def fetch_weather(lat: float, lon: float, date: datetime) -> Optional[Dict]:
    """Placeholder. In Phase 3 this will hit a weather API and return:
    {temperature, temperature_high, temperature_low, weather}.
    """
    return None
